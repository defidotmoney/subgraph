{
  "name": "dev-defi-money-arbitrum",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ dev-defi-money-arbitrum",
    "create-local": "graph create --node http://localhost:8020/ dev-defi-money-arbitrum",
    "remove-local": "graph remove --node http://localhost:8020/ dev-defi-money-arbitrum",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dev-defi-money-arbitrum",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.79.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
